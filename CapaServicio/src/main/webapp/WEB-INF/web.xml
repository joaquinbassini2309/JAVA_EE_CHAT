<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>Chat Empresarial</display-name>

    <!-- Filtro CORS a nivel servlet para interceptar OPTIONS antes de JAX-RS -->
    <filter>
        <filter-name>CorsServletFilter</filter-name>
        <filter-class>seguridad.CorsServletFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>CorsServletFilter</filter-name>
        <url-pattern>/*</url-pattern>
        <dispatcher>REQUEST</dispatcher>
    </filter-mapping>

    <!-- Error page to handle SPA routing (no hash) -->
    <error-page>
        <error-code>404</error-code>
        <location>/index.html</location>
    </error-page>

    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

</web-app>
